# Copy to .env and adjust.
SECRET_KEY=change-me
DEBUG=1
DATABASE_URL=postgres://collegedb:collegedb@localhost:5432/collegedb
# Set to 1 only if Postgres is unavailable (dedup degrades to substring match):
DB_FALLBACK_SQLITE=0
